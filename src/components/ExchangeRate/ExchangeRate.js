import React from "react";

const ExchangeRate = (props) => {
    return (
        <div className="exchange-rate exchange-rate--gray-border">1$ = 3.83 PLN</div>
    );
};

export default ExchangeRate;
